package com.noahhuppert.reflect.messaging.providers;

import android.support.v4.util.LruCache;

public class CacheMessagingProvider {
    // TODO Figure out cache and provider system, something so the cache tries to use an Lru cache or calls the provider to create
}
